# Copy to .env and fill in your ngrok authtoken.
# Get yours at https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=

# GitHub PAT used by the Claude Code GitHub MCP server (user-scope config).
# Not consumed by docker-compose — kept here so secrets have one home.
GITHUB_PAT=
